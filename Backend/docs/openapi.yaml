openapi: 3.0.0

info:
  title: LawBridge API
  description: API documentation for LawBridge backend
  version: 1.0.0

servers:
  - url: http://localhost:3000/api

tags:
  - name: Auth
    description: User token refresh and logout
  - name: Users
    description: User registration, login, profile, and account management
  - name: Lawyers
    description: Lawyer login, profile, and availability management
  - name: Admin
    description: Admin-only operations for managing lawyers and appointments
  - name: Appointments
    description: Booking, listing, cancelling, and paying for appointments
  - name: Documents
    description: Upload and manage legal documents for AI analysis
  - name: Chat
    description: AI legal chatbot session management
  - name: Messages
    description: Sending messages to the RAG chatbot
  - name: Video
    description: Video call token generation and status management

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "An error occurred"
    User:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        email:
          type: string
          format: email
        image:
          type: string
          format: uri
        phone:
          type: string
        address:
          type: object
          properties:
            Location:
              type: string
            City:
              type: string
            State:
              type: string
        gender:
          type: string
        dob:
          type: string
        emailVerified:
          type: boolean
        twoFactorEnabled:
          type: boolean
        credits:
          type: object
          properties:
            dailyLimit:
              type: integer
            remaining:
              type: integer
    Lawyer:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        email:
          type: string
          format: email
        image:
          type: string
          format: uri
        speciality:
          type: string
        degree:
          type: string
        experience:
          type: string
        about:
          type: string
        available:
          type: boolean
        fees:
          type: number
        address:
          type: object
          properties:
            Location:
              type: string
            City:
              type: string
            State:
              type: string
        slots_booked:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
    Appointment:
      type: object
      properties:
        id:
          type: string
        slotDate:
          type: string
          example: "20_06_2025"
        slotTime:
          type: string
          example: "10:00 am"
        amount:
          type: number
        payment:
          type: boolean
        isCompleted:
          type: boolean
        cancelled:
          type: string
          enum: ["Not Cancelled", "Cancelled by User", "Cancelled by Lawyer", "Cancelled by Admin"]
        createdAt:
          type: string
          format: date-time
        videoCall:
          type: object
          nullable: true
          properties:
            status:
              type: string
              enum: ["not_started", "in_progress", "completed", "missed"]
            duration:
              type: integer
            startedAt:
              type: string
              format: date-time
              nullable: true
            endedAt:
              type: string
              format: date-time
              nullable: true
        user:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            image:
              type: string
              nullable: true
        lawyer:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            speciality:
              type: string
              nullable: true
            image:
              type: string
              nullable: true

paths:

  # ─── Auth ───────────────────────────────────────────────────────────────────

  /auth/logout:
    post:
      summary: Logout user (clears refresh token cookie)
      tags: [Auth]
      responses:
        "200":
          description: Logged out successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Logged out successfully"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/refresh:
    post:
      summary: Refresh user access token using refresh token cookie
      tags: [Auth]
      responses:
        "200":
          description: New access token issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  accessToken:
                    type: string
                  user:
                    $ref: '#/components/schemas/User'
        "401":
          description: Refresh token missing or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Users ──────────────────────────────────────────────────────────────────

  /user/signup:
    post:
      summary: Register a new user
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, email, password]
              properties:
                name:
                  type: string
                  example: "John Doe"
                email:
                  type: string
                  format: email
                password:
                  type: string
                  example: "StrongPass@123"
      responses:
        "201":
          description: Signup successful, verification email sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "409":
          description: User already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/login:
    post:
      summary: Login user
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                twoFactorCode:
                  type: string
                  description: Required only if 2FA is enabled
                  example: "123456"
      responses:
        "200":
          description: Login successful or 2FA required
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  accessToken:
                    type: string
                  requires2FA:
                    type: boolean
                  user:
                    $ref: '#/components/schemas/User'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Invalid password or 2FA code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Email not verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Too many 2FA attempts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/verify-email/{token}:
    get:
      summary: Verify user email via token link
      tags: [Users]
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
      responses:
        "201":
          description: Email verified successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        "400":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/resend-verification:
    post:
      summary: Resend email verification link
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
      responses:
        "200":
          description: Verification email resent
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Email already verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Verification email already sent recently
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/forgot-password:
    post:
      summary: Request a password reset email
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
      responses:
        "200":
          description: Reset email sent (or silently ignored if user not found)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Reset email already sent recently
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/reset-password/{token}:
    post:
      summary: Reset password using token from email
      tags: [Users]
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password:
                  type: string
                  example: "NewStrongPass@123"
      responses:
        "200":
          description: Password reset successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/get-profile:
    get:
      summary: Get authenticated user's profile
      tags: [Users]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User profile data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  user:
                    $ref: '#/components/schemas/User'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/upload-signature:
    post:
      summary: Get signed Cloudinary upload params for the profile picture
      description: >
        Returns signed Cloudinary params for a direct browser-to-Cloudinary
        upload (public delivery — avatars are meant to be publicly viewable).
        Call this before /user/update-profile when changing the profile image.
      tags: [Users]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Signed upload params issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  timestamp:
                    type: integer
                  signature:
                    type: string
                  apiKey:
                    type: string
                  cloudName:
                    type: string
                  folder:
                    type: string
                  public_id:
                    type: string
                  resourceType:
                    type: string
                    example: "image"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/update-profile:
    patch:
      summary: Update authenticated user's profile
      description: >
        Profile image is no longer uploaded through this endpoint. Get
        /user/upload-signature, upload the file straight to Cloudinary from
        the browser, then pass the returned secure_url as imageUrl here.
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                phone:
                  type: string
                address:
                  type: object
                  properties:
                    Location:
                      type: string
                    City:
                      type: string
                    State:
                      type: string
                dob:
                  type: string
                gender:
                  type: string
                imageUrl:
                  type: string
                  format: uri
                  description: secure_url returned by the direct Cloudinary upload
      responses:
        "200":
          description: Profile updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/delete-account/request:
    post:
      summary: Request account deletion OTP
      tags: [Users]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OTP sent to email
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: OTP already sent recently
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/delete-account/verify:
    post:
      summary: Verify OTP and permanently delete account
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [otp]
              properties:
                otp:
                  type: string
                  example: "482910"
      responses:
        "200":
          description: Account deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Invalid or expired OTP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/2fa/setup:
    post:
      summary: Initiate 2FA setup — returns QR code and manual key
      tags: [Users]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: QR code and manual key returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  qrCode:
                    type: string
                    description: Base64 data URL of QR code image
                  manualKey:
                    type: string
                    description: Base32 secret for manual entry
        "400":
          description: 2FA already enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/2fa/verify:
    post:
      summary: Verify TOTP code and enable 2FA
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  example: "123456"
      responses:
        "200":
          description: 2FA enabled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Invalid code or 2FA already enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/2fa/disable:
    post:
      summary: Disable 2FA (requires password and current TOTP code)
      tags: [Users]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password, twoFactorCode]
              properties:
                password:
                  type: string
                twoFactorCode:
                  type: string
                  example: "123456"
      responses:
        "200":
          description: 2FA disabled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: 2FA not enabled or validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Invalid password or 2FA code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Appointments ────────────────────────────────────────────────────────────

  /user/book-appointment:
    post:
      summary: Book an appointment with a lawyer
      tags: [Appointments]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lawyerId, slotDate, slotTime]
              properties:
                lawyerId:
                  type: string
                  example: "64f1a2b3c4d5e6f7a8b9c0d2"
                slotDate:
                  type: string
                  example: "20_06_2025"
                slotTime:
                  type: string
                  example: "10:00 am"
      responses:
        "201":
          description: Appointment booked successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Slot not available or lawyer unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/appointments:
    get:
      summary: List all appointments for the authenticated user
      tags: [Appointments]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of user appointments
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  appointments:
                    type: array
                    items:
                      $ref: '#/components/schemas/Appointment'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/cancel-appointment:
    post:
      summary: Cancel a user's own appointment
      tags: [Appointments]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Appointment cancelled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Already cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Forbidden — not the appointment owner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/payment-razorpay:
    post:
      summary: Create a Razorpay order for appointment payment
      tags: [Appointments]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Razorpay order created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  order:
                    type: object
                    description: Razorpay order object
        "400":
          description: Appointment cancelled or not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/verify-razorpay:
    post:
      summary: Verify Razorpay payment signature and mark appointment as paid
      tags: [Appointments]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [razorpay_order_id, razorpay_payment_id, razorpay_signature]
              properties:
                razorpay_order_id:
                  type: string
                razorpay_payment_id:
                  type: string
                razorpay_signature:
                  type: string
      responses:
        "200":
          description: Payment verified successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                    example: "Payment Successful"
        "400":
          description: Invalid signature or missing fields
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Payment verification failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Lawyers ─────────────────────────────────────────────────────────────────

  /lawyer/login:
    post:
      summary: Lawyer login
      tags: [Lawyers]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
      responses:
        "200":
          description: Lawyer logged in successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  accessToken:
                    type: string
                  lawyer:
                    $ref: '#/components/schemas/Lawyer'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Invalid password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/refresh:
    post:
      summary: Refresh lawyer access token using cookie
      tags: [Lawyers]
      responses:
        "200":
          description: New access token issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  accessToken:
                    type: string
                  lawyer:
                    $ref: '#/components/schemas/Lawyer'
        "401":
          description: Refresh token missing or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/logout:
    post:
      summary: Lawyer logout (clears lawyer refresh token cookie)
      tags: [Lawyers]
      responses:
        "200":
          description: Lawyer logged out successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/list:
    get:
      summary: Get all lawyers (public)
      tags: [Lawyers]
      responses:
        "200":
          description: List of lawyers
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  lawyers:
                    type: array
                    items:
                      $ref: '#/components/schemas/Lawyer'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/profile:
    get:
      summary: Get authenticated lawyer's profile
      tags: [Lawyers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Lawyer profile data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  profileData:
                    $ref: '#/components/schemas/Lawyer'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/upload-signature:
    post:
      summary: Get signed Cloudinary upload params for the profile picture
      description: >
        Returns signed Cloudinary params for a direct browser-to-Cloudinary
        upload. Call this before /lawyer/update-profile when changing the
        profile image.
      tags: [Lawyers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Signed upload params issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  timestamp:
                    type: integer
                  signature:
                    type: string
                  apiKey:
                    type: string
                  cloudName:
                    type: string
                  folder:
                    type: string
                  public_id:
                    type: string
                  resourceType:
                    type: string
                    example: "image"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/update-profile:
    patch:
      summary: Update authenticated lawyer's profile
      description: >
        Profile image is no longer uploaded through this endpoint. Get
        /lawyer/upload-signature, upload the file straight to Cloudinary
        from the browser, then pass the returned secure_url as imageUrl here.
      tags: [Lawyers]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                fees:
                  type: number
                about:
                  type: string
                available:
                  type: boolean
                address:
                  type: object
                  properties:
                    Location:
                      type: string
                    City:
                      type: string
                    State:
                      type: string
                imageUrl:
                  type: string
                  format: uri
                  description: secure_url returned by the direct Cloudinary upload
      responses:
        "200":
          description: Profile updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  lawyer:
                    $ref: '#/components/schemas/Lawyer'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/dashboard:
    get:
      summary: Get lawyer dashboard stats and latest appointments
      tags: [Lawyers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Lawyer dashboard data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  dashData:
                    type: object
                    properties:
                      earnings:
                        type: number
                      appointments:
                        type: integer
                      patients:
                        type: integer
                      latestAppointments:
                        type: array
                        items:
                          $ref: '#/components/schemas/Appointment'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/appointments:
    get:
      summary: Get all appointments for the authenticated lawyer
      tags: [Lawyers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of lawyer appointments
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  appointments:
                    type: array
                    items:
                      $ref: '#/components/schemas/Appointment'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/cancel-appointment:
    post:
      summary: Cancel an appointment (lawyer only)
      tags: [Lawyers]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Appointment cancelled by lawyer
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Already cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Forbidden — not the lawyer's appointment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/complete-appointment:
    post:
      summary: Mark an appointment as completed (lawyer only)
      tags: [Lawyers]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Appointment marked as completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Already completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Forbidden — not the lawyer's appointment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /lawyer/change-availability:
    post:
      summary: Toggle own availability (lawyer only)
      tags: [Lawyers]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Availability toggled
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                    example: "Lawyer is now available"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Admin ───────────────────────────────────────────────────────────────────

  /admin/login:
    post:
      summary: Admin login
      tags: [Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
      responses:
        "200":
          description: Admin logged in successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  accessToken:
                    type: string
                  admin:
                    type: object
                    properties:
                      id:
                        type: string
                      email:
                        type: string
                      role:
                        type: string
                        example: "admin"
                      name:
                        type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Invalid admin credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/refresh:
    post:
      summary: Refresh admin access token using cookie
      tags: [Admin]
      responses:
        "200":
          description: New access token issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  accessToken:
                    type: string
                  admin:
                    type: object
        "401":
          description: Refresh token missing or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Invalid admin token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/logout:
    post:
      summary: Admin logout (clears admin refresh token cookie)
      tags: [Admin]
      responses:
        "200":
          description: Admin logged out successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/upload-signature:
    post:
      summary: Get signed Cloudinary upload params for a new lawyer's profile picture
      description: >
        Returns signed Cloudinary params for a direct browser-to-Cloudinary
        upload. Since the lawyer doesn't exist yet at this point, the
        folder/public_id use a random id rather than a lawyerId. Call this
        before /admin/add-lawyer.
      tags: [Admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Signed upload params issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  timestamp:
                    type: integer
                  signature:
                    type: string
                  apiKey:
                    type: string
                  cloudName:
                    type: string
                  folder:
                    type: string
                  public_id:
                    type: string
                  resourceType:
                    type: string
                    example: "image"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/add-lawyer:
    post:
      summary: Add a new lawyer (admin only)
      description: >
        Image is no longer uploaded through this endpoint. Get
        /admin/upload-signature, upload the file straight to Cloudinary from
        the browser, then pass the returned secure_url as imageUrl here.
      tags: [Admin]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, email, password, speciality, degree, experience, about, fees, address, imageUrl]
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
                password:
                  type: string
                speciality:
                  type: string
                  example: "Criminal Law"
                degree:
                  type: string
                  example: "LLB"
                experience:
                  type: string
                  example: "5 Years"
                about:
                  type: string
                fees:
                  type: number
                address:
                  type: object
                  properties:
                    Location:
                      type: string
                    City:
                      type: string
                    State:
                      type: string
                imageUrl:
                  type: string
                  format: uri
                  description: secure_url returned by the direct Cloudinary upload
      responses:
        "201":
          description: Lawyer added successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error or duplicate email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/all-lawyers:
    get:
      summary: Get all lawyers (admin only)
      tags: [Admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of all lawyers
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  lawyers:
                    type: array
                    items:
                      $ref: '#/components/schemas/Lawyer'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/all-appointments:
    get:
      summary: Get all appointments (admin only)
      tags: [Admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of all appointments
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  appointments:
                    type: array
                    items:
                      $ref: '#/components/schemas/Appointment'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/cancel-appointment:
    post:
      summary: Cancel an appointment (admin only)
      tags: [Admin]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Appointment cancelled by admin
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Already cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/dashboard:
    get:
      summary: Get admin dashboard stats and latest appointments
      tags: [Admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  dashData:
                    type: object
                    properties:
                      lawyers:
                        type: integer
                      appointments:
                        type: integer
                      patients:
                        type: integer
                      latestAppointments:
                        type: array
                        items:
                          $ref: '#/components/schemas/Appointment'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/change-availability:
    post:
      summary: Toggle lawyer availability (admin only)
      tags: [Admin]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lawyerId]
              properties:
                lawyerId:
                  type: string
      responses:
        "200":
          description: Availability toggled
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Lawyer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Chat ────────────────────────────────────────────────────────────────────

  /chat/create:
    post:
      summary: Create a new chat session
      tags: [Chat]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sessionId]
              properties:
                sessionId:
                  type: string
                  example: "session_abc123"
      responses:
        "201":
          description: Chat session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                  chatId:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded (10 sessions per hour)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat/get/{sessionId}:
    get:
      summary: Get a chat session by sessionId
      tags: [Chat]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: sessionId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Chat session data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  chats:
                    type: object
                    properties:
                      sessionId:
                        type: string
                      title:
                        type: string
                        nullable: true
                      messages:
                        type: array
                        items:
                          type: object
                          properties:
                            role:
                              type: string
                              enum: [user, assistant]
                            content:
                              type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat/sessions:
    get:
      summary: Get all chat sessions for the authenticated user
      tags: [Chat]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of user chat sessions
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  sessions:
                    type: array
                    items:
                      type: object
                      properties:
                        sessionId:
                          type: string
                        title:
                          type: string
                          nullable: true
                        lastMessage:
                          type: string
                        messageCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat/update-title:
    put:
      summary: Update the title of a chat session
      tags: [Chat]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sessionId, title]
              properties:
                sessionId:
                  type: string
                title:
                  type: string
                  example: "My Legal Query"
      responses:
        "200":
          description: Chat title updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Chat not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded (30 updates per minute)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /chat/delete:
    delete:
      summary: Delete a chat session by sessionId
      tags: [Chat]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sessionId]
              properties:
                sessionId:
                  type: string
      responses:
        "200":
          description: Chat deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded (20 deletes per hour)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Documents ───────────────────────────────────────────────────────────────
  # Uploads go straight from the browser to Cloudinary using a backend-issued
  # signed upload — the file bytes never pass through this API. Documents are
  # stored as Cloudinary "authenticated" (private) assets; reading one back
  # (for the chatbot ingestion pipeline, or the "View Document" UI) always
  # goes through a freshly generated short-lived signed URL, never a stored
  # public URL.

  /documents/upload-signature:
    post:
      summary: Get signed Cloudinary upload params for a document
      description: >
        Step 1 of the upload flow. Validates mimetype/size and the daily
        upload limit (2/day), then returns signed Cloudinary params
        (timestamp, signature, folder, public_id, type=authenticated) that
        the client uses to upload the file directly to Cloudinary.
      tags: [Documents]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filename, mimetype, size]
              properties:
                filename:
                  type: string
                mimetype:
                  type: string
                  example: "application/pdf"
                size:
                  type: integer
                  description: File size in bytes
      responses:
        "200":
          description: Signed upload params issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  timestamp:
                    type: integer
                  signature:
                    type: string
                  apiKey:
                    type: string
                  cloudName:
                    type: string
                  folder:
                    type: string
                  public_id:
                    type: string
                  type:
                    type: string
                    example: "authenticated"
                  resourceType:
                    type: string
                    example: "raw"
                  fileType:
                    type: string
                    example: "pdf"
        "400":
          description: Unsupported file type or size exceeds the per-type limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Daily upload limit reached (2 uploads/day)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /documents/confirm-upload:
    post:
      summary: Confirm a direct Cloudinary upload and run the RAG ingestion pipeline
      description: >
        Step 2 of the upload flow, called after the client has uploaded the
        file straight to Cloudinary using the signed params from
        /documents/upload-signature. The backend verifies the asset actually
        exists and belongs to this user, generates a short-lived signed URL,
        and hands that URL to the Python chatbot service for text extraction
        and embedding (the chatbot fetches the bytes itself; they are never
        re-uploaded through this API).
      tags: [Documents]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [publicId, resourceType, filename, fileType]
              properties:
                publicId:
                  type: string
                resourceType:
                  type: string
                  example: "raw"
                filename:
                  type: string
                fileType:
                  type: string
                  example: "pdf"
      responses:
        "201":
          description: Document processed and saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Document uploaded and processed successfully"
                  document:
                    type: object
                    properties:
                      _id:
                        type: string
                      filename:
                        type: string
                      fileType:
                        type: string
                      chunksStored:
                        type: number
                      createdAt:
                        type: string
                        format: date-time
                  uploadsRemaining:
                    type: integer
        "400":
          description: Invalid publicId, or the uploaded asset could not be found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Daily upload limit reached (2 uploads/day)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: RAG ingestion failed (the Cloudinary asset is deleted automatically on this path)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /documents:
    get:
      summary: Get all uploaded documents for the authenticated user
      description: Returns a list of all documents uploaded by the user with metadata
      tags: [Documents]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of user documents
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  documents:
                    type: array
                    items:
                      type: object
                      properties:
                        _id:
                          type: string
                        filename:
                          type: string
                        fileType:
                          type: string
                        chunksStored:
                          type: number
                        createdAt:
                          type: string
                          format: date-time
                  uploadsToday:
                    type: integer
                  uploadsRemaining:
                    type: integer
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /documents/{id}/signed-url:
    get:
      summary: Get a fresh signed URL to view/download a private document
      description: >
        Documents are stored as private ("authenticated") Cloudinary assets
        with no usable permanent public URL. This returns a short-lived
        (5 minute) signed URL, generated on demand, for the "View Document"
        UI and the chat attachment link. Ownership is checked before the
        URL is generated.
      tags: [Documents]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Signed URL issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  url:
                    type: string
                    format: uri
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Document not found or not owned by this user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── OAuth & Magic Link ──────────────────────────────────────────────────────

  /user/google:
    get:
      summary: Initiate Google OAuth 2.0 login
      description: >
        Redirects to Google OAuth consent screen with PKCE security.
        State and code verifier stored in secure HTTP-only cookies.
      tags: [Users]
      responses:
        "302":
          description: Redirect to Google OAuth consent screen
        "500":
          description: Failed to initiate Google authentication
          content:
            text/html:
              schema:
                type: string

  /user/google/callback:
    get:
      summary: Google OAuth callback handler
      description: >
        Handles OAuth callback, exchanges authorization code for tokens,
        creates or updates user account, and redirects to frontend with one-time code.
      tags: [Users]
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
          description: Authorization code from Google
        - in: query
          name: state
          required: true
          schema:
            type: string
          description: CSRF protection state parameter
      responses:
        "302":
          description: Redirect to frontend with one-time code
        "400":
          description: Invalid or expired OAuth state or missing PKCE verifier
          content:
            text/html:
              schema:
                type: string
        "500":
          description: Google authentication failed
          content:
            text/html:
              schema:
                type: string

  /user/google/exchange:
    post:
      summary: Exchange one-time code for access and refresh tokens
      description: >
        Frontend exchanges the one-time code received from OAuth callback
        for actual JWT access and refresh tokens.
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: One-time code from OAuth callback redirect
      responses:
        "200":
          description: Tokens exchanged successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  accessToken:
                    type: string
                  user:
                    $ref: '#/components/schemas/User'
        "400":
          description: Invalid or expired code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/magic-link:
    post:
      summary: Request magic link for passwordless login
      description: >
        Sends a time-limited magic link to user's email (15-minute expiry, single-use).
        Rate limited to 3 requests per hour per user.
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
                  example: "john@example.com"
      responses:
        "200":
          description: Magic link sent to email
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Magic link sent! Check your email."
        "400":
          description: Validation error or Google user attempting magic link
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Email not verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded (3 requests per hour)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /user/verify-magic-link/{token}:
    get:
      summary: Verify magic link token and authenticate user
      description: >
        Validates magic link token and returns access token with refresh cookie.
        Token expires in 15 minutes and is single-use only.
      tags: [Users]
      parameters:
        - in: path
          name: token
          required: true
          schema:
            type: string
          description: Magic link token from email
      responses:
        "200":
          description: Magic link verified, user authenticated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  accessToken:
                    type: string
                  user:
                    $ref: '#/components/schemas/User'
        "400":
          description: Invalid or expired magic link token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Messages ────────────────────────────────────────────────────────────────

  /message/get-message:
    post:
      summary: Send a message to the RAG chatbot and get a response
      description: >
        Sends a user message to the AI legal chatbot within a session.
        Requires available credits. Credits are decremented per request.
        Rate limited to 30 requests per minute per user.
      tags: [Messages]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sessionId, message]
              properties:
                sessionId:
                  type: string
                  example: "session_abc123"
                message:
                  type: stringOAS 3.0

                  example: "What are my rights as a tenant?"
      responses:
        "200":
          description: Chatbot response returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  response:
                    type: object
                    properties:
                      role:
                        type: string
                        example: "assistant"
                      content:
                        type: string
                  creditsRemaining:
                    type: integer
                    example: 7
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "402":
          description: Insufficient credits
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded (30 messages per minute)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  # ─── Video ───────────────────────────────────────────────────────────────────

  /video/get-token:
    post:
      summary: Get Stream video token and call details for an appointment
      description: >
        Generates a Stream.io token for joining a video call.
        Accessible by both the user and lawyer of the appointment.
        Requires a paid, non-cancelled appointment within the valid join window
        (15 minutes before slot start up to 10 minutes after slot end).
        Rate limited to 5 requests per 5 minutes per user.
      tags: [Video]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId]
              properties:
                appointmentId:
                  type: string
      responses:
        "200":
          description: Stream token and call details returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  token:
                    type: string
                    description: Stream.io user token
                  callId:
                    type: string
                  apiKey:
                    type: string
                  userId:
                    type: string
        "400":
          description: Appointment cancelled or validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized or token invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Not a participant, unpaid, call ended, or outside join window
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /video/update-status:
    post:
      summary: Update video call status (join / leave / end)
      description: >
        Updates the call status for a participant action.
        join marks participant as joined and starts the call timer on first join.
        leave marks participant as left and ends the call when both have left.
        end force-ends the call immediately.
        Rate limited to 20 requests per minute per user.
      tags: [Video]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [appointmentId, action]
              properties:
                appointmentId:
                  type: string
                action:
                  type: string
                  enum: [join, leave, end]
                  example: "join"
      responses:
        "200":
          description: Call status updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /video/call-details/{appointmentId}:
    get:
      summary: Get video call details for an appointment
      tags: [Video]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: appointmentId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call details returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  callDetails:
                    type: object
                    properties:
                      callId:
                        type: string
                        nullable: true
                      roomId:
                        type: string
                        nullable: true
                      status:
                        type: string
                        enum: [not_started, in_progress, completed, missed]
                      startedAt:
                        type: string
                        format: date-time
                        nullable: true
                      endedAt:
                        type: string
                        format: date-time
                        nullable: true
                      duration:
                        type: integer
                      userJoined:
                        type: boolean
                      lawyerJoined:
                        type: boolean
                  canJoin:
                    type: boolean
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "403":
          description: Not a participant of this appointment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "404":
          description: Appointment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
